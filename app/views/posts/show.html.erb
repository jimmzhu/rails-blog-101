<h1><%= @post.title %></h1>

<p>
  <%= @post.text %>
</p>

<% if @post.comments.any? %>
<h2>Comments</h2>
<% end %>

<%
#@post.comments.each do |comment|
#    render comment
#end
# auto-loops and renders using correct partial
%>
<%= render @post.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit Post', edit_post_path(@post) %>
<%= link_to 'Back', posts_path %>
